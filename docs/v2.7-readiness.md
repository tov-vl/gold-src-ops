# GoldSrcOps v2.7 Release Readiness

Review date: 2026-09-13. Status: signed candidate and stable publication,
immutable artifact verification, digest-pinned production rollout, and the
bounded read-only production smoke are complete. The stable GitHub Release is
published.

## Scope

The v2.7 product scope is one Incident Investigation slice integrated in
protected `main` through pull request
[#128](https://github.com/tov-vl/gold-src-ops/pull/128):

- `/operator/incidents/{id}` renders one durable availability incident under
  the existing `Reader` policy.
- The Web host composes the existing incident, server, current-status, and
  bounded snapshot Reader endpoints without adding an API contract.
- Opening and recovery context uses at most 50 snapshots from each 15-minute
  boundary window, de-duplicates overlapping records, and orders them by
  observation time.
- Fleet Triage, the incident list, and per-server history link to the exact
  incident record.
- Missing records, absent retained observations, and unavailable upstream data
  fail without exposing a mutation surface or inventing a causal explanation.

The candidate baseline also carries forward the v2.6 production-acceptance and
stable-publication records merged after the signed v2.6 application revision.
Those commits are part of the candidate review boundary but do not add another
v2.7 product workflow.

This record freezes the release-candidate scope. New API endpoints, incident
acknowledgement or annotation, ownership, export, postmortem generation,
lifecycle commands, schema changes, and infrastructure changes are not part of
`v2.7.0-rc.1`.

## Claim Boundary

The target remains one single-node control plane, one Web instance, and one
controlled external game server. Candidate acceptance can show that an
authenticated user can investigate an already-retained incident in that
reference deployment without disturbing monitoring. It cannot prove root
cause, complete historical retention, high availability, multi-region
resilience, long-term reliability, real-player adoption, or an achieved SLO.

Public documentation may contain revision identifiers, image digests,
workflow links, sanitized counts, and bounded outcomes. Host and provider
identifiers, addresses, credentials, secret aliases, operator identity, raw
database rows, tokens, incident reasons, probe failure reasons, and owner-only
evidence stay outside Git.

## Release Identity

| Property | Release value |
| --- | --- |
| Candidate version | `v2.7.0-rc.1` |
| Stable version | `v2.7.0` |
| Source revision | `4bcde099fdb1a5688b6d664f6e8989674a221be3` |
| API image digest | `sha256:6658e45ef83ee371d7d466c4ede88b2a1f23f40ca7fbb9780ff42e976cbab6e4` |
| Web image digest | `sha256:6c0f88910c39bb6b29dff3848ebb0a1aad255a680ef7ca644f7000b685317e37` |
| Candidate workflow | [#34752528075](https://github.com/tov-vl/gold-src-ops/actions/runs/34752528075) |
| Stable workflow | [#34757277795](https://github.com/tov-vl/gold-src-ops/actions/runs/34757277795) |
| GitHub Release | [GoldSrcOps v2.7.0](https://github.com/tov-vl/gold-src-ops/releases/tag/v2.7.0) |
| Previous API rollback reference | Stable `v2.6.0` digest retained outside Git |
| Previous Web rollback reference | Stable `v2.6.0` digest retained outside Git |

The candidate and stable tags are signed, annotated, and target the same source
revision. The stable workflow promoted the accepted digests without rebuilding
them; neither release channel publishes a mutable `latest` alias.

## Candidate Diff Inventory

The final candidate-head review on 2026-09-13 covered four merge commits from
the signed `v2.6.0` target through
`4bcde099fdb1a5688b6d664f6e8989674a221be3`:

| Classification | Pull requests | Result |
| --- | --- | --- |
| v2.6 production acceptance and stable evidence | #126-#127 | Documentation and evidence closure |
| v2.7 Incident Investigation | #128 | One additive Reader Web workflow using existing APIs |
| v2.7 readiness | #129 | Candidate scope, compatibility, and ordered acceptance contract |

No undeclared application workflow or migration was found. The signed
`v2.7.0-rc.1` tag resolves to this exact reviewed candidate head.

## Compatibility Review

- No EF Core migration or persistence model change exists in the candidate
  diff. A fresh backup, restore rehearsal, migration timing exercise, and
  previous-runtime schema rehearsal are therefore not routine v2.7 gates.
- Existing API routes and mutation contracts are unchanged. The Web client
  composes existing Reader endpoints with bounded query parameters.
- The investigation route is static server-side rendering behind the existing
  `Reader` policy. Operator access follows the existing role hierarchy, while
  Reader sessions receive no mutation form.
- Current server status is explicitly separated from the historical incident;
  missing retained snapshots remain missing evidence rather than an inferred
  root cause.
- Ordinary rollback recreates API and Web from the retained v2.6 digests. The
  database and all non-application containers remain unchanged.

## Readiness Matrix

| Gate | Current result |
| --- | --- |
| Repository scope | Incident Investigation integrated through PR #128; readiness through PR #129; production acceptance through PR #130 |
| Scope and compatibility review | Passed final four-commit inventory through `4bcde09` |
| Repository checks | `Quality Gate`, `Container Smoke`, and `Browser Smoke` passed on the exact tag revision in [workflow #34752528075](https://github.com/tov-vl/gold-src-ops/actions/runs/34752528075) |
| Existing Reader APIs | Reused without a new endpoint or response contract |
| Web composition boundary | Passed incident, server, status, and bounded snapshot client checks |
| Authorization and missing data | Passed Reader, Operator, missing-record, and unavailable-data coverage |
| Browser behavior | Passed incident navigation plus desktop, mobile, and token-boundary checks |
| Immutable candidate images | Passed API/Web publication, OCI metadata, and published-image verification by exact digest |
| Production rollout | Passed; only API and Web were recreated, with the v2.6 rollback pair retained |
| Bounded production smoke | Passed a 10 minute 1 second read-only control-plane interval, 16 minute 44 second game-host continuity check, and authenticated Operator UI checks |
| `API-01` | Continues independently; candidate work must not reset or reinterpret its window |
| Stable publication | Passed digest-preserving API/Web promotion and published-image verification in [workflow #34757277795](https://github.com/tov-vl/gold-src-ops/actions/runs/34757277795); [GitHub Release](https://github.com/tov-vl/gold-src-ops/releases/tag/v2.7.0) published |

## Ordered Release Sequence

### 1. Repository Closure

1. Merge this readiness record through protected `main`.
2. Repeat the exact `v2.6.0..main` commit and file inventory.
3. Require `Quality Gate`, `Container Smoke`, and `Browser Smoke` on the final
   candidate revision.

### 2. Candidate Publication

1. Create signed annotated tag `v2.7.0-rc.1` at the reviewed `main` revision.
2. Require build-mode publication of both API and Web images from that exact
   revision.
3. Record and independently verify both immutable digests, OCI revision
   metadata, and published-image smoke jobs.
4. Do not create a mutable alias or stable GitHub Release at this stage.

### 3. Digest-Pinned Rollout

1. Retain the current v2.6 API and Web digests in owner-only rollback
   configuration.
2. Capture a sanitized continuity baseline for public health, container and
   game-service identities, restart counts, A2S state, incidents, queues, and
   scheduled-backup freshness.
3. Run production preflight against the verified candidate digests.
4. Recreate only API and Web. Do not recreate Caddy, PostgreSQL, telemetry, or
   the game service, and do not run a schema-changing operation.

### 4. Short Production Smoke

Run one read-only 10-15 minute session and stop on the first unexplained
failure:

1. Require public API liveness, API readiness, and Web health to return HTTP
   `200`, with candidate version, revision, and exact image digests.
2. Verify anonymous routes remain sanitized and protected routes still require
   authentication.
3. Use an existing authenticated Operator session, which includes Reader
   access, to navigate from the fleet or incident history to one already-retained
   incident. Do not create an incident for release evidence.
4. Verify the page renders the exact durable record, distinct current-server
   context, and the available opening and recovery observations. Missing
   retained observations must be presented as missing evidence.
5. Confirm the page exposes no mutation form and browser content or storage
   contains no bearer token, credential value, secret reference, or command
   payload. If unchanged OIDC makes a fresh Reader-only login unnecessary,
   reuse Reader authorization and concealment evidence from Browser Smoke on
   the exact Web digest and record that substitution explicitly.
6. Require the controlled server to remain recently polled and reachable with
   zero bots. Require no unexpected container or game-service restart, newly
   opened incident, pending outbox item, dead letter, or incomplete command.
7. Recheck public health and scheduled-backup freshness at the end.

This short smoke is sufficient for the additive read-only Web change. It is not
a new soak, recovery rehearsal, lifecycle acceptance, or SLO claim. Existing
passive monitoring and the independent `API-01` window continue without user
participation.

## Accepted Candidate Evidence

The digest-pinned rollout and bounded production smoke passed on 2026-09-13:

- Public API liveness and readiness, Web liveness, and the Web root returned
  HTTP `200`; anonymous API access remained `401` and the protected Web route
  remained a login redirect.
- API and Web reported `2.7.0-rc.1`, the exact tagged revision, and the exact
  accepted image digests. The migration count and latest migration were
  unchanged.
- All seven control-plane containers remained running. Configured health checks
  were healthy, restart counts matched their baselines, and Caddy, PostgreSQL,
  and all telemetry container identities were unchanged.
- The single enabled server remained online, reachable, and recently polled
  with zero bots. No unreachable poll, newly opened or open incident, pending
  outbox item, dead letter, or incomplete command appeared during the interval.
- Scheduled PostgreSQL backup evidence remained fresh and alert delivery
  remained disabled.
- The game service remained active and boot-disabled with its baseline restart
  count, invocation, and main process unchanged. Exactly one configured UDP
  listener remained owned by the service control group.
- An authenticated Operator session navigated from Fleet Triage and server
  history to an already-retained resolved incident. The investigation view kept
  current Online context separate from the durable record and rendered opening
  and recovery observations without a mutation.
- Browser content and storage exposed no bearer-token-shaped value, secret
  reference, or password field. The only POST form was logout, and the browser
  console emitted no warning or error.

A dedicated Reader production login was not repeated. Reader rendering and
mutation-control concealment passed `Browser Smoke` on the same immutable Web
image, while production OIDC configuration and authorization policies were
unchanged. This is an explicit bounded-evidence substitution that avoids
another interactive login ceremony; it is not evidence of a fresh Reader-only
production session.

The first guard invocation compared the tag-form version `v2.7.0-rc.1` with the
OCI version label `2.7.0-rc.1`. The identity check rejected that operator
parameter mismatch and automatically restored the retained v2.6 API and Web
pair. The corrected invocation then passed and retained the candidate. No
candidate defect or non-application service discontinuity was observed.

Sanitized control-plane, browser, and game-host evidence is retained outside
Git as root-owned mode `0600` regular files. The control-plane interval ran from
`2026-09-13T11:31:01.1409258Z` through `2026-09-13T11:41:02.5585603Z` and
recorded 11 successful samples.

## Failure And Rollback Rules

- Do not deploy if publication, digest verification, preflight, or the
  continuity baseline fails.
- On candidate startup, health, OIDC, authorization, data-boundary, or rendering
  failure, recreate API and Web together from the retained v2.6 digests.
- Do not mutate the database, monitoring state, RCON state, incident history,
  or durable queues to make the smoke pass.
- A bad or missing availability minute remains bad. Do not pause, reset,
  backfill, or reinterpret `API-01`.
- Store sanitized smoke evidence outside Git as an owner-only regular file.

## Stable Publication Gate (Completed 2026-09-13)

Signed stable tag `v2.7.0` targets the accepted candidate revision.
[Workflow #34757277795](https://github.com/tov-vl/gold-src-ops/actions/runs/34757277795)
skipped the API and Web build paths, promoted the exact accepted digests, and
independently smoke-tested both stable refs. Independent registry inspection
then resolved both stable refs to the candidate digests before the
[GitHub Release](https://github.com/tov-vl/gold-src-ops/releases/tag/v2.7.0)
was published.

## References

- [GoldSrcOps v2.7.0](https://github.com/tov-vl/gold-src-ops/releases/tag/v2.7.0)
- [Stable publication workflow](https://github.com/tov-vl/gold-src-ops/actions/runs/34757277795)
- [v2.7 release notes](release-notes-v2.7.md)
- [Project backlog](backlog.md)
- [Project brief](project-brief.md)
- [v2.6 release readiness](v2.6-readiness.md)
- [Production Compose contract](../ops/production/README.md)
- [Security](security.md)
- [Service-level objectives](service-level-objectives.md)
