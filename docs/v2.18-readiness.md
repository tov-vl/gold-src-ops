# GoldSrcOps v2.18 Release Readiness

Review date: 2026-09-18. Status: product pull request merged. Candidate
publication, production acceptance, stable promotion, and evidence closure are
pending.

## Scope

The v2.18 release scope is Pending Delivery Triage:

- a Reader-authorized bounded pending-delivery endpoint;
- versioned opaque cursor pagination in existing claim-index order;
- safe retry and linked incident/server display metadata;
- explicit visibility for missing incident links;
- a static-rendered Reader page and a link from Alert Delivery Overview;
- focused application, cursor, PostgreSQL, authorization, Reader-client, SSR,
  token-boundary, and desktop/mobile browser coverage.

The slice adds no migration, background worker, retry-policy change,
authorization role, production identity, mutation permission, delivery
activation, receiver configuration, event producer, or game-host artifact.

## Claim Boundary

Acceptance can show that the exact candidate returns naturally existing pending
rows through the Reader policy, preserves claim-index ordering, renders only the
reviewed safe fields, and does not mutate queue state. It can also show public
health, exact candidate identity, runtime continuity, and parity with bounded
owner-only queue evidence.

Acceptance cannot prove dispatcher or receiver liveness, delivery success,
historical queue behavior, retry correctness under failure, alert latency,
long-term reliability, high availability, or an achieved SLO. It must not
change production data to manufacture pagination, a missing relation, a retry,
or any other visual state.

## Candidate Identity

| Property | Candidate value |
| --- | --- |
| Candidate version | `v2.18.0-rc.2` |
| Candidate source revision | Pending inbox-race fix merge |
| API image digest | Pending candidate publication |
| Web image digest | Pending candidate publication |
| Candidate workflow | Pending |
| Previous rollback boundary | Stable `v2.17.0` digests retained owner-only |

The signed `v2.18.0-rc.1` tag targeted product merge `238332d`. Its
[candidate workflow](https://github.com/tov-vl/gold-src-ops/actions/runs/35330590912)
stopped in the pre-publication quality gate when the existing PostgreSQL game
event inbox race test exposed an idempotency-classification window. No image was
published and no production rollout began. The tag remains immutable; the
fix is validated before publication as a new `rc.2` candidate.

The signed stable tag will target the exact accepted candidate revision. Stable
publication must promote the candidate API and Web digests without rebuilding.

## Compatibility And Risk Class

This is an R1 additive read-only API/Web release:

- existing routes and response contracts are unchanged;
- API and Web are the only affected runtime components;
- the query uses the existing `IX_outbox_messages_pending_claim` order and adds
  no schema or persistence-model change;
- the endpoint and page reuse Reader authorization and expose no mutation;
- no backup, restore rehearsal, migration rehearsal, game-host activation,
  gameplay capture, Auth0 change, RCON action, alert delivery activation, queue
  replay, or queue drain is a release gate.

## Repository Gates

| Gate | Current result |
| --- | --- |
| Solution build | Passed locally with zero warnings and errors |
| Focused application/API tests | Passed locally |
| PostgreSQL pending-query translation and safe projection | Passed locally |
| Reader client and static-rendering tests | Passed locally |
| Chromium token boundary and desktop/mobile layout | Passed locally |
| Product pull request and required CI | Passed in PR `#180` |
| Post-merge main CI | Passed for `238332d` |
| Concurrent inbox idempotency regression | Fix validation in progress |

The product pull request supplies the single complete repository, container,
and browser gate. An unchanged tree is not subjected to another equivalent full
local run merely to duplicate green CI.

## Production Acceptance Plan

1. Capture a sanitized read-only baseline for public API/Web health, container
   identities and restart counts, queue aggregates, incidents, commands, A2S,
   bot count, backup freshness, and relevant dependency continuity.
2. Deploy only the independently verified API and Web candidate digests.
3. Collect at least three healthy read-only samples spanning at least three
   minutes.
4. Verify candidate version, revision, exact digests, public health, zero
   unexpected restarts, and unaffected runtime continuity.
5. Through an existing Reader session, verify Alert Delivery Overview and
   Pending Delivery Triage against bounded owner-only queue metadata.
6. Verify anonymous API access remains unauthorized and page output contains no
   payload, delivery error, claim identity, destination, address, credential,
   token, or mutation control.
7. Stop and restore the recorded v2.17 API/Web digests on the first unexplained
   failure. Do not mutate durable state to make the check pass.

## Stable Promotion And Closure

After acceptance, create signed `v2.18.0` at the exact candidate revision and
use the stable workflow to promote both candidate digests without rebuilding.
Verify the stable references, published-image smoke, and GitHub Release. Then
use one short evidence pull request to replace pending values in this record,
update release notes and the README release pointer, link workflows and the
release, and record any anomaly without private infrastructure data.

This readiness record defines bounded release evidence. A short production
smoke is not proof of long-term reliability or an achieved SLO.

## References

- [v2.18 release notes](release-notes-v2.18.md)
- [Release process](release-process.md)
- [Deployment](deployment.md)
- [Alert delivery operations](alert-delivery.md)
- [Security](security.md)
- [Project backlog](backlog.md)
