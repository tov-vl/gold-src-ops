# v2.4 Reader Portal

## Status

The first authenticated Web slice was deployed on 2026-09-06 as signed
candidate `v2.4.0-rc.3` from revision `cb4bf4f`. Pull request
[#87](https://github.com/tov-vl/gold-src-ops/pull/87) introduced the slice, and
[workflow 34026555320](https://github.com/tov-vl/gold-src-ops/actions/runs/34026555320)
passed the repository gates plus API and Web image publication and digest
verification. This is release-candidate evidence, not a stable v2.4 release.
The anonymous public-status contract is unchanged.

## Scope

An authenticated `Reader` or `Operator` can:

- sign in and sign out through the external OIDC provider;
- list controlled servers and their enabled state;
- inspect the latest status, A2S reachability, map, players, latency, last
  success, and consecutive failures for one server.

The slice has no mutation controls. Server registration, configuration, RCON
commands, dead-letter replay, and all other Operator actions remain API-only.

## Security Boundary

GoldSrcOps.Web acts as a confidential OIDC client and server-side BFF. It uses
authorization code with PKCE, applies the `Reader` policy to the ID-token
principal, and forwards the access token only on server-side calls to the API.
The browser receives an HTTP-only opaque session cookie and never receives the
API access token.

The identity provider must place the configured namespaced role claim in both
the ID token and access token. `Reader` and `Operator` can render the portal;
the API independently validates the access-token issuer, audience, lifetime,
and role for every request.

The process-local ticket store is bounded to 1,024 sessions. It deliberately
invalidates sessions on restart and supports only the current single Web
instance. A shared encrypted ticket store is a prerequisite for horizontal Web
scaling, not for this MVP slice.

## Production Inputs

The OIDC provider must register:

- application type: regular/confidential Web application;
- callback URL: `https://goldsrcops.com/signin-oidc`;
- logout callback URL: `https://goldsrcops.com/signout-callback-oidc`;
- authorization-code grant with PKCE;
- no public sign-up beyond the existing operator-managed account policy.

Compose maps only three Web-specific secrets: the OIDC client secret, a PKCS #12
Data Protection certificate, and its password. The key ring is stored in the
dedicated UID `1654`, mode `0700` host directory. Direct secret configuration is
accepted only in Development.

## Verification

Repository tests cover local-return URL validation, ticket storage, renewal,
expiry, concurrent capacity enforcement and removal, production secret
constraints, mandatory production key persistence, bearer forwarding,
disabled-authentication fail-closed behavior, the Reader/Operator/no-role
policy matrix, and authenticated rendering of both Reader pages. The production
container smoke additionally verifies that authentication-disabled images
expose neither the login endpoint nor protected content.

The production deployment passed these live checks on 2026-09-06:

1. Reader login, server list, server detail, and logout succeeded.
2. Operator rendered the same read-only list and detail pages.
3. A valid account without either role received access denied.
4. Callback and logout redirects remained HTTPS through Caddy.
5. The rendered portal DOM contained no JWT-shaped value or token-named field.
   The exact deployed revision keeps tokens in the bounded server-side ticket
   store and configures the browser cookie as opaque, `HttpOnly`, `Secure`, and
   `SameSite=Lax`.
6. A controlled Web restart cleared the process-local ticket store. Existing
   provider SSO transparently established a new session, while public Web and
   API health continued to return HTTP 200.
7. The controlled server remained online with a fresh zero-bot observation;
   no open incident, pending outbox message, dead letter, or incomplete command
   was present, and scheduled PostgreSQL backup evidence remained fresh.
8. The game-server service remained active and boot-disabled with unchanged
   restart, invocation, and process baselines plus one owned UDP listener.

The expanded live API policy matrix passed through the public production route
on 2026-09-06 at `12:58:43Z` against `v2.4.0-rc.3` from revision `cb4bf4f`.
All ten outcomes matched the contract: anonymous, expired, foreign-issuer, and
wrong-audience requests returned `401`; Reader read and metrics requests
returned `200`; Reader mutation and valid no-role requests returned `403`;
Operator read returned `200`; and an authenticated but invalid Operator
registration request reached request validation and returned `400`. The Auth0
access-token lifetimes were restored to `3600/3600`, and the dedicated test
identity was restored to its direct `Reader` role after the bounded checks.
No token or target-specific evidence was written to Git.

Before stable v2.4 publication, one bounded check remains:

1. Add a dedicated automated browser assertion that local storage, session
   storage, response bodies, and the opaque session-cookie attributes preserve
   the documented token boundary.
