# GoldSrcOps v2.5 Release Readiness

Review date: 2026-09-12. Status: signed candidate publication, immutable
artifact verification, digest-pinned production rollout, Stage A read-only,
Stage B reversible-state, outbox-closure, and Stage C disruptive-command
acceptance are complete. Exact-digest stable promotion remains pending.

## Scope

The v2.5 product scope contains the six guarded lifecycle slices already
integrated in protected `main`:

1. Monitoring pause and resume.
2. Idempotent paused server registration.
3. Paused non-secret server editing with optimistic concurrency.
4. RCON credential binding and rotation by validated deployment alias.
5. Fixed server restart.
6. Strictly validated map change.

The candidate repository baseline also includes post-v2.4 operational
maintenance already integrated in `main`: WireGuard-scoped SSH preflight
hardening, availability shadow-audit tooling and policy alignment, and v2.4
release and `API-01` evidence updates. These carried-forward changes do not add
another v2.5 product workflow, but every commit remains inside the candidate
review and verification boundary.

This readiness record freezes that scope. Raw RCON, bulk operations, server
deletion, secret-value management, multi-node execution, and additional UI
features are not pulled into `v2.5.0-rc.1`.

## Claim Boundary

The target remains one single-node control plane, one controlled external game
server, and one Web instance. Candidate acceptance can prove the guarded
workflows against that reference environment. It cannot prove high
availability, multi-region resilience, long-term reliability, real-player
adoption, or an achieved SLO.

Public documentation may contain revision identifiers, image digests, workflow
links, sanitized counts, and bounded outcomes. Host and provider identifiers,
addresses, credentials, secret aliases, operator identity, raw request or
command payloads, database rows, and owner-only evidence stay outside Git.

## Candidate Identity

| Property | Candidate value |
| --- | --- |
| Candidate version | `v2.5.0-rc.1` |
| Source revision | `c8000c090cb0de03f848ec4e999bcf4505332b93` |
| API image digest | `sha256:ebbecc713e71e879f72234588274354efc8da9d08a266290e8bd1da8248c58da` |
| Web image digest | `sha256:b59313b35ed6c408b952f6f0a31b3311918cd2d10c28759b6ab17a8478c7191d` |
| Candidate workflow | [GitHub Actions run #34586962203](https://github.com/tov-vl/gold-src-ops/actions/runs/34586962203) |
| Previous API rollback reference | Stable `v2.4.0` digest retained outside Git |
| Previous Web rollback reference | Stable `v2.4.0` digest retained outside Git |

The identity table was completed from the successful tag workflow and the
verified signed tag target. Stable publication must promote these exact digests
without inferring from a mutable alias or rebuilding either image.

## Candidate Diff Inventory

The candidate-head review on 2026-09-11 covered the 16 merge commits from the
signed `v2.4.0` target through
`c8000c090cb0de03f848ec4e999bcf4505332b93`:

| Classification | Pull requests | Result |
| --- | --- | --- |
| Host preflight hardening | #101 | Operational tooling only |
| Availability shadow audit and policy | #102-#106 | Operational tooling, workflow, and evidence documentation |
| v2.4 closure and `API-01` records | #107, #114-#115 | Release and evidence documentation |
| v2.5 lifecycle product scope | #108-#113 | The six declared API and Web workflows |
| v2.5 release readiness | #116 | Scope freeze, compatibility review, and rollout contract |

No additional application workflow was found. The signed tag targets this exact
reviewed candidate head.

## Migration SQL Review

The three v2.5 migrations were scripted locally in order with EF Core 10.0.7
against the PostgreSQL 16 deployment baseline. The generated SQL:

- adds two nullable registration-idempotency columns and a unique index to
  `servers`;
- adds `Revision bigint NOT NULL DEFAULT 1` to `servers` and
  `server_credentials`;
- renames the existing server-credential uniqueness index without changing its
  indexed columns; and
- records each migration in its own transaction.

There is no explicit row-by-row backfill. PostgreSQL 16 can apply the constant
revision defaults without a heap rewrite, but each `ALTER TABLE` still needs a
brief exclusive metadata lock. The non-concurrent unique-index build blocks
writes to `servers` while it runs. Production migration therefore remains a
serialized maintenance action, and the isolated restore rehearsal must measure
its actual duration on a fresh production backup.

The schema changes are additive for the previous v2.4 runtime. Ordinary
application rollback retains the migrated schema; running the down migrations
would remove v2.5 data and is not the normal rollback path. The retained v2.4
API started successfully against the migrated rehearsal database through a
read-only connection with background workers disabled.

## Readiness Matrix

| Gate | Current result |
| --- | --- |
| Six repository slices | Implemented and integrated through pull requests #108-#113 |
| Scope and compatibility review | Candidate-head inventory completed through signed revision `c8000c0`; no undeclared application workflow found |
| Repository checks | Passed on the exact candidate revision in [workflow #34586962203](https://github.com/tov-vl/gold-src-ops/actions/runs/34586962203): `Quality Gate`, `Container Smoke`, and `Browser Smoke` |
| Additive migration review | Passed: generated SQL reviewed locally; fresh isolated restore, already-up-to-date migration reapplication, and previous v2.4 read-only startup and health all passed |
| Immutable candidate images | Passed: signed `v2.5.0-rc.1` built and independently digest-verified the API and Web images in workflow #34586962203 |
| Backup and restore | Passed on 2026-09-11: fresh encrypted off-host backup, `100%` repository data check, and network-isolated candidate restore with matching owner-only evidence |
| Public and OIDC regression | Passed on 2026-09-11: public health and anonymous boundaries passed, followed by the ten-case Reader, Operator, missing-role, expired-token, issuer, and audience matrix |
| Lifecycle read boundaries | Passed on 2026-09-11: Reader mutation concealment and Operator review-only flows were verified, protected browser content remained sanitized, and the durable-state projection was unchanged |
| Reversible lifecycle mutation | Passed on 2026-09-12: confirmed pause stopped polling, same-alias rebind advanced only the expected server and credential revisions, confirmed resume succeeded, and four healthy zero-player/zero-bot polls followed. The rebind used an authenticated Operator API fallback; live Web submission evidence is explicitly deferred below. |
| Disruptive command mutation | Passed on 2026-09-12: one fixed restart, one alternate-map change, and one separate original-map restore reached `Succeeded`; three, two, and two healthy scheduled polls respectively followed with zero players and bots. |
| Production continuity | Passed after Stage C: exact candidate version, revision, and image digests match; all seven containers are healthy with zero restarts; game-service restart, invocation, main-process, and owned-listener continuity passed; public health and backup freshness passed; no open incident, active outbox item, dead letter, or incomplete command remains. |
| `API-01` | Independently `Collecting`; first terminal review remains after `2026-09-17T16:50:00Z` |
| Stable publication | Ready to propose; requires a signed tag at the accepted revision, exact-digest promotion without rebuild, published-image verification, and GitHub Release publication |

## Candidate Recovery Evidence

The v2.5 recovery and migration-compatibility gates passed on 2026-09-11
without modifying the production database or running application services:

- A fresh encrypted PostgreSQL backup was written to the configured off-host
  repository and followed by an authenticated `100%` data check.
- The recoverable backup was restored into disposable PostgreSQL with no
  network interface. The migration bundle came from the exact candidate API
  digest recorded above.
- The migrated copy contained 11 EF Core migration records, all 8 required
  application tables, and the expected single controlled-server record.
- Reapplying the candidate migration bundle to the already-migrated copy
  completed successfully and left the migration-history count unchanged at 11.
- The retained previous v2.4 API started against the same migrated copy through
  a read-only database connection. Polling, command dispatch, snapshot
  retention, alert delivery, and telemetry export were disabled; liveness and
  readiness passed without a published host port or network egress.
- Backup and restore evidence matched the same snapshot identity and remained
  outside Git as root-owned `0600` files. Snapshot and provider identifiers are
  intentionally omitted here.
- The complete restore, migration, reapplication, and previous-runtime check
  completed in approximately 18 seconds. This small single-node data set is an
  initial recovery-time observation, not an RTO commitment.
- The temporary environment copy, containers, and volumes were removed. The
  production environment and deployed v2.4 images remained unchanged; all
  seven container restart counts remained zero and public health returned HTTP
  `200`.
- Production preflight passed against the exact recorded candidate API and Web
  digests using the merged deployment tooling. It validated configuration and
  image metadata without performing a runtime rollout.

These results closed the deployment-prerequisite gate before the production
rollout. The later rollout and Stage A outcomes are recorded below.

## Production Rollout And Stage A Evidence

The exact `v2.5.0-rc.1` API and Web digests recorded above were deployed on
2026-09-11 through the bounded production sequence:

- The candidate migration bundle ran as a serialized one-shot action and left
  the production migration history at the expected 11 records.
- Only API and Web were recreated. Caddy, PostgreSQL, the observability
  services, and the game host were left unchanged; all seven control-plane
  containers remained running with zero restarts.
- Public API and Web health, release metadata, anonymous authentication
  boundaries, rollback-reference retention, and disabled alert delivery all
  passed the strict post-rollout verifier.
- The ten-case live authorization matrix passed for anonymous, Reader,
  Operator, missing-role, expired-token, foreign-issuer, and wrong-audience
  boundaries. The single Operator mutation probe stopped at request validation
  and created no durable state.
- Reader views exposed no mutation controls. Operator inventory, history,
  command, incident, dead-letter, configuration, credential, and all six v2.5
  review flows rendered without a final submission. The post-review durable
  projection exactly matched its pre-review baseline.
- The test-only Reader-role and token-lifetime changes used to obtain negative
  authorization fixtures were immediately restored. The Reader identity was
  verified without Operator access, and both API token lifetimes were re-read
  as `3600` seconds after restoration.
- The game service remained active and boot-disabled with the same invocation
  and process identity, zero restarts, and one owned UDP listener. The single
  controlled server remained online and recently polled with zero bots. There
  were no open incidents, pending outbox messages, dead letters, incomplete
  commands, or rollout anomalies, and scheduled backup evidence remained fresh.
- Sanitized rollout, Stage A, and OIDC results remain outside Git as root-owned
  `0600` evidence. Tokens, credentials, command payloads, database rows, host
  addresses, and provider identifiers are not recorded here.

This closes the digest-pinned rollout and Stage A gates. No lifecycle mutation
was submitted during Stage A; the later Stage B result is recorded separately
below. Stage C was executed as the distinct follow-up action recorded below.

## Stage B Reversible-State Evidence

Stage B completed on 2026-09-12 against the same digest-pinned candidate:

- The controlled server was online with zero players and bots, no open incident,
  and no incomplete command before the first mutation.
- A confirmed Web action paused monitoring and advanced the server revision from
  `1` to `2`. The latest poll timestamp and snapshot count then remained exact
  for more than three poll intervals, with no new incident.
- The production API returned the fresh paused server and credential revisions.
  Source and integration-test review confirmed that the settings and credential
  review models bind those freshly loaded values. The final credential review
  POST was not submitted through the live Web page in this bounded session and
  is not claimed as production UI write-path evidence.
- The existing deployment alias was read independently without exposing its
  value. A single authenticated Operator API fallback rebound that same alias
  with expected revisions `2` and `1`; the server and credential revisions
  advanced to `3` and `2`, while endpoint metadata and the effective deployment
  binding remained unchanged. The fallback verifies the production API path,
  not the deferred Web submission; the guarded Web review remains the supported
  operator UI path.
- A new confirmed Web action resumed monitoring and advanced the server revision
  to `4`. Four subsequent scheduled polls were observed, exceeding the required
  three; every snapshot was reachable with zero players and bots.
- Stage B created no incident, outbox, command, or dead-letter record. A brief
  incident had opened and recovered between Stage A and Stage B, before the
  pause. Its two outbox records were deliberately closed before Stage C as
  described below.
- The game service remained active and boot-disabled with the same invocation
  and process identity, unchanged restart count, and exactly one UDP listener
  owned by its service control group. All seven control-plane containers remained
  healthy with zero restarts, public liveness and readiness returned `200`, and
  scheduled backup evidence remained fresh.
- Sanitized Stage B evidence is stored outside Git on both hosts as root-owned
  `0600` files. It contains no host address, provider identifier, server UUID,
  token, credential value, secret reference, command payload, or database row.

Stage B therefore passes with the explicit Web-submission evidence deferral
above. The deferral narrows the live credential-review evidence, but it does not
weaken the API authorization, optimistic-concurrency, same-alias, durable-state,
or integration-test results.

## Outbox Closure And Stage C Evidence

The two inherited availability records were dispositioned before disruptive
commands. The first temporary receiver used a source-IP matcher that rejected
the address observed at the public boundary, and both records moved from
pending to dead letter after one attempt. No direct database mutation or
automatic retry was used. The receiver boundary was replaced with a random
one-time header-protected HTTPS endpoint that denied unauthenticated public
access. An authenticated Operator replay was then submitted exactly once for
each dead letter. Both records reached `Processed`, the replay audit delta was
exactly two, pending and dead-letter counts returned to zero, and alert delivery
was restored to disabled. API and Caddy were temporarily recreated before the
Stage C baseline, but their candidate images did not change. The closure result
is deliberately classified as `PassedWithRecoveredBoundaryFailure`, not as an
incident-free pass.

Stage C ran from `2026-09-12T11:41:57Z` through `2026-09-12T11:48:58Z`:

- Precondition checks reconfirmed healthy A2S, zero players and bots, a valid
  alias-only credential binding, no open incident, and no incomplete command.
- The fixed restart reached `Succeeded` and was followed by three healthy
  scheduled polls.
- One installed alternate map was selected dynamically. Its change reached
  `Succeeded` and was followed by two healthy scheduled polls.
- A separate confirmed command restored the original map, reached `Succeeded`,
  and was followed by two further healthy scheduled polls.
- All seven control-plane containers retained the identities and images
  recorded at the Stage C baseline and remained at zero restarts. Public API
  liveness and readiness and the Web endpoint remained healthy.
- The game service remained active and boot-disabled. Its restart count,
  invocation identity, and main process were unchanged, and exactly one UDP
  listener remained owned by its service. Required maps remained installed.
- The final projection contained no open incident, pending or processing outbox
  item, dead letter, or incomplete command. Scheduled backup evidence remained
  fresh.

Sanitized owner-only evidence is retained outside Git at
`/var/lib/goldsrcops/evidence/v2.5-rc1-outbox-closure.json` and
`/var/lib/goldsrcops/evidence/v2.5-rc1-stage-c.json` on the control plane, and
`/var/lib/goldsrcops/evidence/v2.5-rc1-stage-c-game-baseline.json` and
`/var/lib/goldsrcops/evidence/v2.5-rc1-stage-c-game.json` on the game host. Each
final evidence file is a regular root-owned `0600` file. Independent final
auditors validated the canonical fields without emitting host addresses,
provider identifiers, credentials, command payloads, or database rows.
Post-acceptance regression passed 11 focused PostgreSQL command and dead-letter
replay tests plus 28 guarded restart and map-change Web workflow tests.

## Rollout Sequence

The production evidence must preserve this order.

### 1. Repository Closure (Completed 2026-09-11)

1. Inventory the exact diff against stable `v2.4.0`. Classify the
   carried-forward operational and evidence changes separately from the six
   declared lifecycle slices, and reject any undeclared application behavior.
2. Inspect generated migration SQL for table rewrites, blocking index work,
   defaults, and rollback compatibility.
3. Pass formatting, build, unit and PostgreSQL integration tests, browser tests,
   NuGet audit, production Compose validation, and API/Web container smoke.
4. Merge through protected `main` before creating the signed candidate tag.

### 2. Candidate Publication (Completed 2026-09-11)

1. Create signed annotated tag `v2.5.0-rc.1` at the reviewed `main` revision.
2. Require build-mode publication of both API and Web images from that exact
   revision.
3. Record both immutable digests and verify OCI revision metadata plus the
   published-image smoke jobs.
4. Do not create a mutable alias or stable release at this stage.

### 3. Deployment Prerequisites (Completed 2026-09-11)

1. Retain the current v2.4 API and Web digests in owner-only rollback
   configuration.
2. Capture sanitized pre-rollout continuity: public health, container and game
   service identities, restart counts, A2S state, incidents, queues, and backup
   freshness.
3. Completed 2026-09-11: create a fresh encrypted off-host PostgreSQL backup
   and run a full repository check.
4. Completed 2026-09-11: restore into disposable network-isolated PostgreSQL,
   apply and reapply the migration bundle from the candidate API image, and
   start the retained v2.4 API against that migrated copy through a read-only
   connection.
5. Completed 2026-09-11: run host and production preflight against the exact
   candidate API and Web digests without changing the production runtime. A
   failed or interrupted migration still blocks runtime rollout.

### 4. Digest-Pinned Rollout (Completed 2026-09-11)

1. Apply the candidate migration bundle as the serialized one-shot operations
   action.
2. Recreate only API and Web with the verified candidate digests. Keep Caddy,
   PostgreSQL, telemetry, and the game host unchanged.
3. Require public liveness and readiness, Web health, correct release metadata,
   OIDC metadata, and previous rollback references before continuing.
4. Verify zero unexpected container restarts, a healthy controlled server, and
   no new incident, pending outbox message, dead letter, or incomplete command.

### 5. Live Acceptance

Run the stages below serially. Stop on the first unexplained failure or uncertain
mutation outcome.

#### Stage A: Read-Only Boundaries (Completed 2026-09-11)

- Confirm anonymous public pages remain sanitized and protected routes require
  authentication.
- Confirm Reader and Operator sessions can inspect the inventory, status,
  settings, credential metadata, monitoring state, and command readiness.
- Confirm Reader receives no mutation forms or submission endpoints, while a
  missing-role identity fails closed.
- Confirm protected browser responses and storage contain no bearer token,
  secret, secret reference, or command payload.
- Open every v2.5 Operator review flow without submitting it, then verify that
  durable counts remain unchanged.

All Stage A checks passed in the bounded production session described above.

#### Stage B: Reversible State (Completed 2026-09-12)

- Require the controlled server to be online with zero players, zero bots, no
  open incident, and no `Pending` or `Running` command.
- Pause monitoring through one confirmed Operator action. Verify the server
  revision advances and scheduled polling stops without manufacturing an
  availability incident.
- While paused, verify the settings and credential review pages use the freshly
  loaded revisions. Do not change endpoint metadata merely to produce release
  evidence.
- When the existing deployment alias is independently available, rebind that
  same alias once and verify only sanitized credential metadata and revision
  changes. Skip the write rather than introduce a temporary secret alias.
- Resume monitoring through a new one-time confirmation and require at least
  three subsequent successful scheduled polls.

The checks above passed with a documented authenticated Operator API fallback
and live Web-submission evidence deferral for the same-alias rebind. Pause and
resume used separate confirmed Web actions. See `Stage B Reversible-State
Evidence` above for the exact evidence boundary.

Do not submit live registration for a disposable endpoint. The route,
authorization, review, and browser boundary are checked in production; the
idempotent write and concurrency behavior remain covered by PostgreSQL-backed
tests until a real second server is intentionally onboarded or a retirement
workflow exists.

#### Stage C: Disruptive Commands (Completed 2026-09-12)

- Reconfirm zero players, zero bots, empty incomplete-command state, healthy
  A2S, and a valid RCON binding before each command.
- Queue one fixed restart, observe one durable terminal result, and require A2S
  recovery plus at least three healthy scheduled polls. Do not retry an
  uncertain result.
- Record the current map, choose a known installed map that passes the strict
  allowlist, queue one map change, and require one durable terminal result plus
  A2S recovery. Restore the original map only as a separate confirmed action.
- After both checks, require no unexpected service replacement or container
  restart, no open incident, no pending outbox work, no dead letter, no
  incomplete command, and a fresh scheduled backup marker.

This is one bounded acceptance session, not a new multi-day soak. API-01
continues to provide independent forward-looking availability evidence.

All Stage C checks above passed. The original map was restored through its own
confirmed command, and the final control-plane and game-host audits passed.

## Failure And Rollback Rules

- Never start runtime after a failed or interrupted migration action.
- Roll back API and Web together to the retained v2.4 digests when candidate
  startup, health, OIDC, or read-boundary checks fail.
- Keep the additive v2.5 schema during ordinary application rollback. Use a
  database restore only after a diagnosed migration or data-integrity failure.
- Never repeat an uncertain registration, update, credential, restart, or map
  mutation automatically. Reload current state and durable history first.
- A failed disruptive command blocks stable v2.5 publication but does not
  rewrite prior v2.4 evidence or the API-01 target.
- A rollout-time bad or missing primary availability minute remains bad. Do not
  pause, reset, or backfill the prospective API-01 window.

## Stable Publication Gate

Stable `v2.5.0` may be proposed only when:

1. The readiness matrix contains evidence for every required candidate gate or
   an explicit accepted deferral that narrows the claim.
2. The candidate has passed the bounded production acceptance session and the
   reference environment is healthy afterward.
3. The signed stable tag points to the accepted candidate revision and promotes
   the exact verified API and Web digests without rebuilding them.
4. Published-image verification passes before the GitHub Release is published.

The API-01 terminal review is a parallel post-release obligation and is not a
reason to reinterpret or extend this feature gate.

The first two stable-publication conditions now pass. The remaining release
action is condition 3 followed by condition 4: create the signed stable tag at
the accepted candidate revision, promote the exact verified images without a
rebuild, verify them, and publish the GitHub Release.

## MVP Verification Cadence

The default acceptance depth after v2.5 is proportional to the change:

- UI-only slices use CI, focused browser coverage, and one 10-15 minute bounded
  production smoke.
- Backend slices without schema or infrastructure impact add focused
  integration checks and keep the same short production window.
- Schema, infrastructure, security-boundary, or recovery changes retain their
  specific migration, backup, restore, and preflight checks.
- A 24-hour soak is reserved for host or network topology, persistence and
  recovery, telemetry delivery, or a major release milestone. It is not a
  routine gate for every MVP slice.
- Passive health, queue, backup-freshness, and availability checks should run
  automatically and remain quiet while healthy. User participation is limited
  to unavoidable OIDC or MFA ceremonies and separately approved irreversible
  or financial actions.

## References

- [v2.5 release notes](release-notes-v2.5.md)
- [Project backlog](backlog.md)
- [Project brief](project-brief.md)
- [RCON operations](rcon.md)
- [Security](security.md)
- [Production Compose contract](../ops/production/README.md)
- [PostgreSQL backup and restore](postgresql-backup.md)
- [Service-level objectives](service-level-objectives.md)
