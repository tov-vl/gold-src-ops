# GoldSrcOps v2.3 Controlled Game-Server Baseline

## Status

This document defines the provider-independent selection, provisioning, network,
recovery, and evidence contract for one controlled Counter-Strike 1.6 server. A
self-operated MyArena game VDS is the bounded-trial target selected in
`docs/v2.3-gameserver-provider-decision.md`. The host is provisioned and its
reviewed foundation passed a controlled reboot and post-reboot audit on
2026-09-01. The plan-first pinned runtime installer was then applied and its
recorded identity and hashes were independently verified. The rollback-safe
activation workflow then installed the protected configuration, started the
service without enabling it across reboot, and passed its current-invocation
configuration-load gates. MyArena game protection is configured for the
reviewed Counter-Strike 1.6 UDP endpoint. External A2S, source-address
preservation, authenticated `rcon_users`, secret containment, and a short
seven-query stability gate have passed from the production control plane. A
production Operator request has also registered the reviewed endpoint, and the
singleton poller stored three successful snapshots over 120 seconds with zero
failures and zero bots. On 2026-09-02, a controlled stop, deliberate
configuration withdrawal, atomic restore, and start completed successfully. The
subsequent post-restart check covered 31 scheduled A2S snapshots across 30
minutes and 2 seconds with zero failures and zero bots. A disposable local
target-runtime rehearsal completed on 2026-08-29; it remains compatibility
evidence rather than a substitute for this external runtime evidence.

The broader production recovery exercise completed on 2026-09-02. A separate
controlled stop produced three consecutive failed polls, exactly one open
incident, and one durable unavailable event. Restoring the same service closed
that incident after 195 seconds and produced the corresponding recovered event.
The unit finished active but disabled, with one UDP listener, zero automatic
restarts, and zero bots. Sanitized evidence remains outside Git.

The first baseline deliberately excludes YaPB and other synthetic players. A
standalone 24-hour no-bot gate will not be run for Slice 2. This accepted scope
decision does not turn the short post-restart check into an availability claim:
YaPB remains excluded, and zero-bot evidence was retained through the completed
integrated 24-hour deployment soak.

## Goal

Operate one project-owned ReHLDS and ReGameDLL_CS server outside the
GoldSrcOps control-plane host, then verify the existing A2S polling and guarded
RCON workflows across a real UDP network boundary.

This slice proves the protocol and operational boundary. It does not add a
hosting-provider adapter or make GoldSrcOps responsible for billing,
provisioning, file management, or process lifecycle APIs.

## Entry Conditions

- The operator owns or administers the selected game server.
- The provider permits the pinned engine and GameDLL versions, guarded RCON,
  backups, and later synthetic bot load.
- The environment exposes a stable IPv4 A2S endpoint.
- RCON can be reached through a private tunnel or restricted to an explicitly
  approved control-plane source address.
- Server and provider credentials remain outside Git, image layers, command
  history, logs, screenshots, and public evidence.

If the host cannot restrict RCON to an approved source, A2S-only exploration
may continue, but Slice 2 cannot be declared complete.

## Candidate Runtime

The initial candidates were rechecked against the upstream non-prerelease
releases on 2026-08-28:

| Component | Candidate | Release asset | SHA-256 |
| --- | --- | --- | --- |
| ReHLDS | [`3.15.0.896`](https://github.com/rehlds/ReHLDS/releases/tag/3.15.0.896) | `rehlds-bin-3.15.0.896.zip` | `997baeb7ef3842dab3e034d82dc651ebfe560c23b158adce660a6b97976b4e2b` |
| ReGameDLL_CS | [`5.30.0.814`](https://github.com/rehlds/ReGameDLL_CS/releases/tag/5.30.0.814) | `regamedll-bin-5.30.0.814.zip` | `457f5c96a4d10280fcad47f106cbd5be86363a47df1ccab91c514e9d1bc6fd18` |

The official Linux SteamCMD bootstrap observed on 2026-09-01 has SHA-256
`cebf0046bfd08cf45da6bc094ae47aa39ebf4155e5ede41373b579b8f1071e7c`.
Its official URL and the `steam_legacy` HLDS depot are mutable distribution
channels, not immutable release assets. `ops/gameserver/runtime-install.sh`
therefore rejects changed bootstrap bytes and records the actual Steam build id
and app-manifest hash delivered during an approved installation.

These exact candidates passed the local rehearsal below, but they are not
completed deployment evidence. Before installation:

1. Download each exact release asset and its detached `.asc` signature from the
   upstream release.
2. Recompute SHA-256 locally and compare it with the recorded release digest.
3. Verify the detached signature with the upstream release-signing key.
4. Record the final engine, GameDLL, base dedicated-server build, operating
   system, architecture, asset hashes, and verification time in the Slice 2
   evidence.
5. Reject mutable `latest` downloads, unversioned mirrors, and provider images
   whose actual binaries cannot be inspected.

An upstream update discovered before provisioning requires a reviewable version
and compatibility decision. It must not silently replace these candidates.

## Local Target-Runtime Rehearsal

On 2026-08-29, the candidate versions were exercised in a disposable
`linux/amd64` Docker environment before any provider purchase. The build used
the [`jstnmthw/rehlds-docker`](https://github.com/jstnmthw/rehlds-docker)
harness at commit `f194e45043b9ea783d40292ddcdce39305424ee4` and the pinned
`steam_legacy` HLDS branch.

| Check | Sanitized result |
| --- | --- |
| Release integrity | Both ZIP files matched the SHA-256 values above. Both detached signatures were valid for ReHLDS Team key fingerprint `63547829004F07716F7BE4856C32C4282E60FB67`. |
| Runtime identity | Debian GNU/Linux 13, `linux/amd64`, protocol 48, HLDS `1.1.2.7/Stdio/4419`, ReHLDS `3.15.0.896`, and ReGameDLL_CS release `5.30.0.814`. The ReGameDLL startup banner rendered its version as `5.30.0.814-dev`. |
| Startup and A2S | The server became healthy on `de_dust2`. Direct loopback `A2S_INFO` returned `0/4` players, `0` bots, protocol 48, Linux environment, and the expected game metadata. |
| Guarded RCON | The authenticated `-WhatIf` preflight queued nothing. One generated `say` command then reached persisted `Succeeded` state with a temporary Operator identity, start and completion timestamps, and no failure reason. |
| Secret hygiene | Random disposable database and RCON credentials were kept outside Git. Exact-value checks found neither credential in the API logs, and all temporary settings, processes, containers, images, and files were removed after the run. |

The harness image also packages Metamod-R, AMX Mod X, ReAPI, YaPB, and Reunion.
YaPB and Reunion were disabled for this run, while the remaining plugin stack
was still present. Consequently, this rehearsal proves local compatibility of
the pinned engine and GameDLL with the existing GoldSrcOps A2S and RCON paths;
it does not satisfy the minimal no-plugin baseline, an external network path,
RCON source restriction, or a live restart and restore on the selected host.

## Provider Selection Gate

The managed-hosting or self-operated target is acceptable only when all of the
following are available:

- explicit control over the ReHLDS and ReGameDLL_CS binaries;
- a stable IPv4 address or DNS name and a known UDP game/query port;
- a fixed or controllable source boundary for RCON;
- process stop, start, restart, and status operations;
- configuration and log access without exposing the RCON password in routine
  output;
- exportable configuration backups or a provider-native backup with a tested
  restore path;
- a documented policy for platform updates so pinned files are not overwritten
  without notice;
- enough ownership to run a later YaPB experiment without misrepresenting bots
  as real users.

Reject a target that requires a proprietary application integration, prevents
artifact verification, exposes unrestricted RCON, or cannot restore its
configuration independently of the live instance.

The provider name, region, price, account identifiers, and management endpoint
are deployment records, not application configuration. Only sanitized,
portfolio-relevant details belong in public repository evidence.

The first comparison selected a MyArena KVM game VDS for one bounded trial, with
Timeweb Cloud retained as the fallback. The non-deferred purchase-time gates and
approved checkout are complete. The first controlled reboot retained management
reachability, but full-period address stability and DDoS source-address
preservation remain mandatory empirical gates under
`docs/v2.3-gameserver-provider-decision.md`.

## Network Contract

| Source | Destination | Purpose | Required control |
| --- | --- | --- | --- |
| Public clients and approved probes | Game-server UDP endpoint | Gameplay and `A2S_INFO` | Public only when intentionally operated as a public server; otherwise allowlisted for the baseline |
| Approved baseline probe or control-plane source | Game-server UDP endpoint | RCON challenge, command, and response | Private tunnel preferred; otherwise exact source-IP restriction and accepted plaintext-UDP risk |
| Operator workstation | Provider management plane or host SSH | Provisioning, restart, backup, and recovery | Provider MFA or SSH keys; never exposed through GoldSrcOps |
| Game-server host | External update sources | Initial installation or an approved update window | Deny unnecessary outbound access after provisioning when the platform permits it |

GoldSrc normally carries gameplay, A2S, and RCON through the same UDP server
endpoint. A port firewall therefore may not distinguish a public A2S request
from an RCON packet. When a private tunnel is unavailable, combine the provider
firewall where useful with the ReHLDS RCON source allowlist and one approved
control-plane egress address. Before the Slice 3 VPS exists, a temporary
operator-controlled source may be approved for the baseline and revoked after
verification. Final production evidence must repeat the check from the VPS.
Do not compensate for a failed smoke test by opening RCON to the Internet.

ReHLDS provides `rcon_adduser <ipaddress/CIDR>`, `rcon_deluser`, and
`rcon_users` for this application-layer restriction, as documented in the
[`3.15.0.896` README](https://github.com/rehlds/ReHLDS/blob/3.15.0.896/README.md).
An empty RCON user list allows any source that knows the password; it is not a
deny-all state. Configure the approved IPv4 source as an exact `/32`, verify a
non-empty list with `rcon_users`, and repeat that verification after every
restart. If the provider startup path does not persist the allowlist safely,
Slice 2 remains incomplete.

The residual RCON boundary is explicit: its password and command traffic are
not transport-encrypted by the legacy protocol. Source restriction reduces
exposure but does not provide confidentiality or cryptographic peer identity.

## Configuration And Secrets

Tracked evidence may contain:

- component versions, release URLs, hashes, and signature-verification result;
- sanitized configuration names and a configuration diff with secret values
  removed;
- intended A2S visibility, port numbers, source-restriction method, and backup
  ownership;
- GoldSrcOps server id, command id, terminal status, snapshot times, and
  incident ids;
- restart and restore timings without provider account identifiers.

The following must remain in the selected secret boundary:

- RCON password;
- provider credentials, recovery codes, and account identifiers;
- SSH and tunnel private keys;
- non-public management endpoints;
- GoldSrcOps bearer tokens and secret-provider values.

Generate a high-entropy RCON password compatible with the existing client,
which rejects double quotes. Store the value on the server and under the
dedicated `RconSecrets:<alias>` application setting only. PostgreSQL retains
the canonical `rcon-secret://<alias>` reference, never the password.

## Live Host Foundation Evidence

On 2026-09-01, the reviewed `ops/gameserver` bootstrap was applied from its
versioned source. It completed dependency-bearing Ubuntu security updates,
installed the minimal 32-bit prerequisites, created the locked `goldsrc` service
identity and owner-scoped paths, enabled unattended updates, and applied the
documented kernel settings. A controlled reboot then verified the installed
security kernel, key-only operator access, disabled root and interactive SSH,
the exact-source UFW rules, synchronized UTC time, a clean package state, the
readiness marker, and zero failed systemd units. Only SSH and local system
services were listening; no game process or runtime secret existed.

This closes provisioning step 2 only. Addresses, provider identifiers, key
fingerprints, and raw command output remain in private deployment evidence.

## Live Runtime Installation Evidence

On 2026-09-01, the reviewed runtime installer was transferred from the merged
revision and its local and target SHA-256 values matched. Plan-only mode left the
empty target unchanged. Apply then verified the pinned SteamCMD bootstrap and
the checksum plus detached signature for ReHLDS `3.15.0.896` and ReGameDLL_CS
`5.30.0.814`. SteamCMD stabilized HLDS app `90` after four passes and recorded
build id `5433925`.

An independent post-install audit verified the root-owned marker, all 12
artifact and systemd-unit hashes, runtime ownership, excluded plugin and bot
paths, the constrained unit, active firewall, and zero failed systemd units. The
unit is `disabled` and `inactive`; `runtime-enabled`, public/private
configuration, game process, and UDP listener are absent. This closes
provisioning steps 3 through 5 without crossing the activation boundary. Target
addresses, provider identifiers, fingerprints, and raw output remain outside
Git.

## Live Runtime Activation And Network Evidence

On 2026-09-01, the reviewed activation workflow was transferred from its merged
revision and matched locally and on the target before apply. It reverified the
host and installed-runtime identities, accepted the escrowed RCON secret only
through stdin, installed the root-controlled public/private configuration, and
required both configuration-load markers from the current systemd invocation.
The service became `active` while remaining `disabled`, with zero restarts.

The provider console initially had no protected game service, so UDP queries
left the control plane without reaching the game-host interface. Adding the
Counter-Strike 1.6 protection profile for only the reviewed game UDP port closed
that external boundary. Synchronized captures then observed one request and one
response on each host, with the production control-plane source preserved. The
GoldSrcOps A2S client returned protocol 48, capacity four, and zero bots. A
subsequent short stability gate completed seven of seven queries while the
service remained active, disabled, and at zero restarts.

The existing GoldSrcOps RCON client then executed read-only `rcon_users` from
the production control plane. The response was non-empty, contained the exact
approved source, and contained no world allowance. The escrowed secret matched
both protected private configuration copies and was absent from process
arguments, process environment, the systemd unit, public configuration,
installation markers, and the current invocation journal. Temporary probes,
captures, and staging files were removed after verification.

This closes the controlled activation, external A2S, source-preservation, and
approved-source RCON preflight gates. At that point, the guarded `say`,
controlled restart/restore, and trial-period address-stability evidence still
remained.
Addresses, provider identifiers, secret material, and raw output remain outside
Git.

## Live Production Registration And Polling Evidence

On 2026-09-01, a fresh Operator Device Flow authenticated the production API
without persisting its bearer token. The reviewed external endpoint was
registered once with a 60-second polling interval. The singleton production
poller then stored three distinct reachable snapshots across 120 seconds. Every
observed snapshot had no failure reason and reported zero bots; the final current
state was reachable with zero consecutive failures.

No RCON command was queued during this step. The temporary local runner was
removed after completion, while bearer tokens, addresses, server identifiers,
response bodies, and raw logs remained outside Git. This closes endpoint
registration and scheduled polling only. At that point, it did not satisfy the
guarded write, restart/restore, or address-stability gates.

## Live Restart And Restore Evidence

On 2026-09-02, the service was stopped cleanly and its UDP listener disappeared.
The public configuration, private configuration, and activation marker were
then deliberately withdrawn from their expected paths. Each file was restored
atomically from a root-only recovery copy, with the private configuration first
and the activation marker last. File ownership, modes, and SHA-256 values matched
the pre-restart baseline before the service was started again.

One A2S snapshot taken three seconds after systemd reported the service active
caught the startup transition and was unreachable. The post-restart observation
therefore began at the first healthy snapshot one minute later. From that point,
31 scheduled snapshots spanned 1,802 seconds with zero failed snapshots, zero
invalid bot counts, and a final reachable state with zero consecutive failures.
The service remained active and disabled, with one unchanged invocation, one
unchanged process, one UDP listener, and `NRestarts=0`. Both configuration-load
markers were present in the new invocation, excluded plugin and bot paths stayed
absent, and read-only `rcon_users` again confirmed a non-empty exact-source
allowlist with no world allowance.

This is a short post-restart recovery check, not an availability SLO or the
broader v2.3 recovery exercise. Temporary recovery copies and diagnostic files
were removed after verification. Addresses, provider identifiers, credentials,
raw responses, and raw logs remain outside Git.

## Provisioning Sequence

1. Select a target against the provider gate and record any accepted residual
   risk before purchase or provisioning.
2. Apply the reviewed, plan-first
   [`ops/gameserver` host bootstrap](../ops/gameserver/README.md), reboot when
   required, and reverify key-only operator access plus the allowlisted
   firewall boundary.
3. Require an empty project runtime target. If provider-supplied or manually
   installed files are present, stop and capture a recoverable snapshot or
   export before a separate reviewed replacement workflow.
4. Create a minimal Counter-Strike 1.6 dedicated server with ReHLDS and
   ReGameDLL_CS only through the reviewed plan-first installer in
   [`ops/gameserver`](../ops/gameserver/README.md). The installer must leave its
   constrained systemd unit disabled and inactive. Do not install YaPB, AMX Mod
   X, ReAPI, gameplay plugins, or custom content during the baseline.
5. Verify the `runtime-installed` identity and binary hashes before creating
   configuration or enabling the service. Record the exact runtime versions
   reported by the server after the later controlled start.
6. Configure the server identity, one known map, logging bounds, RCON secret,
   and source restriction. Keep the secret out of shell history and evidence.
7. Apply the network controls and verify from an unapproved source, when a safe
   test source is available, that RCON access is not accepted.
8. Start the server and freeze the sanitized baseline configuration.
9. Verify A2S from an independently routed probe outside the game-server host.
   Repeat the same check from the final control-plane VPS after Slice 3.
10. Register the endpoint in a local GoldSrcOps instance, observe scheduled
   polling, and run exactly one guarded `say` command.
11. Exercise the documented stop/start and configuration-restore procedure,
    then repeat the A2S check.
12. Keep bot count at zero through the later 24-hour deployment soak before
    considering YaPB. A separate 24-hour Slice 2 gate is not required.

The stop/start rehearsal is an intentional operation. It must have a rollback
path and should not be combined with an engine upgrade or another configuration
change.

## External A2S Verification

Run the protocol diagnostic from a non-loopback source outside the game-server
host, replacing the placeholder with the reviewed endpoint:

```powershell
dotnet run --project .\src\GoldSrcOps.A2SSpike -- `
  <game-server-host>:<query-port> `
  --timeout 5000 `
  --encoding windows-1251
```

Record the resolved endpoint, sanitized server name, map, engine response,
player count, bot count, protocol, version, and latency. Post-restart checks and
the later deployment soak require the A2S bot count to remain zero. Player
counts observed during operator testing are test traffic, not adoption evidence.

Run the final external-path evidence from the actual control-plane VPS. An
independently routed secondary probe may supplement that result but cannot
replace it.

Next, follow `docs/smoke-test.md` to register the same endpoint in a local
GoldSrcOps instance. Evidence must show repeated scheduled snapshots rather
than only one successful diagnostic request.

## Guarded RCON Verification

For local Slice 2 evidence, keep the GoldSrcOps API on loopback and let only its
UDP target be remote. This preserves the JWT protection in
`tools/smoke/rcon-live.ps1`, which refuses to transmit bearer tokens to a
non-loopback API. Production evidence may use the public API only after its
hostname, trusted TLS certificate, issuer, audience, and Operator role have been
verified. Keep the access token in memory, perform the same target and credential
preflight, reject an existing identical command, dispatch once, and never retry
an ambiguous timeout without first reading the persisted command record.

Run the authenticated preflight first:

```powershell
.\tools\smoke\rcon-live.ps1 `
  -ServerId "<registered-server-id>" `
  -AcknowledgeOwnedServer `
  -WhatIf
```

After checking the displayed name, endpoint, and generated command, run the
same helper without `-WhatIf`. Type the server id again when prompted. Record
only the returned command id, server id, terminal status, elapsed time, and the
persisted audit identity.

Do not retry after a timeout until the persisted command record is inspected.
GoldSrc RCON has no idempotency key, so an ambiguous timeout can follow a
successful remote execution.

## Baseline Acceptance

Slice 2 is complete only when evidence demonstrates all of the following:

- exact ReHLDS, ReGameDLL_CS, and base server builds plus verified release
  artifacts;
- a documented provider or host ownership boundary and recoverable sanitized
  configuration;
- external A2S success followed by repeated GoldSrcOps polling over the same
  non-loopback network path;
- one guarded `say` command with a persisted Operator identity and `Succeeded`
  terminal status;
- RCON restricted to the approved path, with any lack of transport
  confidentiality explicitly accepted, and a non-empty ReHLDS RCON user list
  verified again after restart;
- a successful controlled stop/start and configuration restore;
- a post-restart observation lasting at least 30 minutes from the first healthy
  A2S result, with no failed scheduled snapshots, bots, unexplained restarts, or
  process replacement;
- no provider credential, RCON password, bearer token, private key, or private
  management endpoint in Git, logs, screenshots, or public telemetry.

YaPB remains deferred after this acceptance set and the integrated 24-hour
deployment soak completed. Any later YaPB evidence must report bot and total
player counts separately.

The standalone 24-hour interval was removed from the Slice 2 evidence plan by an
accepted scope decision on 2026-09-02. The completed 30-minute check proves
immediate recovery stability only; the broader integrated 24-hour deployment
soak subsequently passed as part of the v2.3 release gate. That release soak
covers the full control plane and game-server boundary, not only bot absence.

## Stop Conditions

- Stop before RCON if source restriction or a private path cannot be proved.
- Stop before installation if a release hash or detached signature fails.
- Stop and rotate credentials if a secret appears in output or evidence.
- Stop provider evaluation if pinned runtime files are replaced automatically
  without a controllable update policy.
- Stop command testing after an ambiguous timeout; inspect durable state before
  deciding on any manual retry.
- Stop a post-restart observation or later soak after an unexplained restart,
  version drift, bot appearance, or configuration change. Resolve the cause and
  restart the affected observation.

## Next Evidence Step

The single guarded production `say`, controlled stop/start, atomic configuration
restore, repeated A2S polling, and post-restart `rcon_users` verification have
completed. The production observability and broader controlled recovery slices
have also passed. Trial-period address stability and the integrated 24-hour
deployment soak completed successfully with zero bot-positive polls; there is
no separate no-bot-only step. Record only sanitized terminal state, recovery
result, address-stability outcome, and observation interval; provider
identifiers, command identifiers, credentials, tokens, addresses, and raw logs
remain outside Git.
