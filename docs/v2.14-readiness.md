# GoldSrcOps v2.14 Release Readiness

Review date: 2026-09-17. Status: candidate preparation. No v2.14 tag,
immutable image, production rollout, or stable release has been published yet.

## Scope

The v2.14 product scope is one Scoped Activity Investigation slice integrated
in protected `main` through pull request
[#166](https://github.com/tov-vl/gold-src-ops/pull/166):

- `GET /api/dashboard/activity` accepts optional `serverId` and validated
  `kind=all|incidents|commands|gameplay` filters while retaining the existing
  bounded response shape.
- Server and source filters run before each source-specific `Take(limit)`, then
  the merged result keeps the existing global newest-first limit.
- The static-rendered Reader page adds a controlled-server selector and
  preserves the selected server across type tabs, refresh, and empty-result
  navigation; `Clear server` intentionally removes that scope.
- Summary and tab counts come from one all-source bounded snapshot for the
  selected server, while a selected type uses a separate bounded query for its
  rows.
- Invalid activity kinds return a validation problem instead of silently
  widening the query.

The slice adds no schema migration, response-body field, authorization-policy
change, background worker, production identity, mutation permission, event
producer, delivery activation, or game-host artifact.

This record freezes the release-candidate scope. Pagination, unbounded history,
saved filters, alert delivery, gameplay-detail expansion, schema change,
infrastructure change, or an unrelated UI slice is not part of
`v2.14.0-rc.1` acceptance.

## Claim Boundary

Candidate acceptance can show that an authenticated Reader can narrow the
bounded Recent Activity investigation to one controlled server and one event
type without exposing additional fields or mutating production state. It
cannot prove complete history, long-term retention, high availability,
multi-region resilience, user adoption, long-term reliability, or an achieved
SLO.

Public documentation may contain revision identifiers, image digests,
workflow links, sanitized counts, durations, and pass/fail results. Host and
provider identifiers, addresses, credentials, secret paths or aliases,
operator identity, raw database rows, tokens, source identities, event
identifiers, and RCON content stay outside Git.

## Candidate Identity

| Property | Candidate value |
| --- | --- |
| Candidate version | `v2.14.0-rc.1` |
| Integrated product revision | `467d63a612d595ed85314305a6ac535507ed98b3` |
| Candidate source revision | Pending: reviewed protected `main` revision after this readiness record merges |
| API image digest | Pending candidate publication |
| Web image digest | Pending candidate publication |
| Candidate workflow | Pending candidate publication |
| Previous API rollback reference | Stable `v2.13.0` digest retained outside Git |
| Previous Web rollback reference | Stable `v2.13.0` digest retained outside Git |

The candidate source revision and immutable digests must be copied from the
successful signed-tag workflow before rollout. Stable publication must promote
those exact candidate digests without rebuilding either image.

## Candidate Diff Inventory

The review covers the signed `v2.13.0` target
`cc80b747c377630ee354607b19e0967ac65152a8` through integrated product
revision `467d63a612d595ed85314305a6ac535507ed98b3`:

| Classification | Pull request | Result |
| --- | --- | --- |
| v2.13 candidate acceptance | #163 | Sanitized exact-digest rollout and bounded production evidence |
| v2.13 stable publication | #164 | Exact-digest promotion and release closure |
| Version-neutral release process | #165 | Risk classes, bundled approvals, reduced gates, and timing targets |
| v2.14 Scoped Activity Investigation | #166 | Additive Reader API filtering and static-rendered UI |
| v2.14 release readiness | Pending | Candidate identity, minimal rollout path, rollback rules, and claim limits |

Before tagging, repeat the exact `v2.13.0..main` commit and file inventory and
replace the pending candidate source revision with the resulting reviewed
`main` commit. The executable diff contains no migration, persistence model,
worker, production configuration, secret handling, OIDC, or game-host change.

## Compatibility And Reduced Gate

- Existing callers may omit both new query parameters and receive the same
  bounded all-server, all-source response as before.
- The response body is unchanged. Existing clients do not need a contract
  update to ignore the new request options.
- Filtering before each source limit prevents newer unrelated records from
  displacing an older matching result.
- The page remains static server-side rendering behind the existing `Reader`
  policy. Operator access follows the existing role hierarchy.
- API and Web must be published and deployed together because the Web client
  sends the new optional query parameters.
- No EF Core migration or persistence change exists. A fresh backup, restore
  rehearsal, migration timing exercise, and previous-runtime schema rehearsal
  are therefore not v2.14 gates.
- No game-host artifact changed. Plugin activation, gameplay capture, RCON
  action, or a game-service soak would not add release-relevant evidence.
- Pull request #166 and its post-merge workflow already exercised the full
  application, container, and browser suites. This readiness pull request uses
  the fail-closed documentation-only fast path; the candidate tag still runs
  the complete release workflow against the exact source revision.

## Readiness Matrix

| Gate | Current result |
| --- | --- |
| Repository scope | Scoped Activity Investigation integrated through PR #166 |
| Product pull request CI | Passed `Change Scope`, `Quality Gate`, `Container Smoke`, and `Browser Smoke` in workflow [#35204950777](https://github.com/tov-vl/gold-src-ops/actions/runs/35204950777) |
| Post-merge CI | Passed the same four required checks in workflow [#35205688559](https://github.com/tov-vl/gold-src-ops/actions/runs/35205688559) |
| Per-source filtering and limiting | Passed focused repository and endpoint integration coverage |
| Request validation and forwarding | Passed API and Reader client coverage |
| Reader behavior | Passed server selection, all four type tabs, refresh, explicit scope clearing, and empty-state navigation coverage |
| Browser behavior | Passed desktop/mobile responsive Chromium and browser token-boundary coverage |
| Final documentation checks | Pending readiness pull request CI |
| Immutable candidate images | Pending signed `v2.14.0-rc.1` publication and independent digest verification |
| Production rollout | Pending exact-digest API/Web-only recreation |
| Bounded production smoke | Pending short read-only verification |
| Stable publication | Pending exact-digest promotion and GitHub Release |

## Ordered Release Sequence

### 1. Repository Closure

1. Merge this readiness record through protected `main`.
2. Repeat the exact `v2.13.0..main` commit and file inventory and record the
   reviewed candidate revision.
3. Require `Change Scope`, `Quality Gate`, `Container Smoke`, and `Browser
   Smoke` on the documentation-only readiness revision. The required check
   names remain present while the docs fast path avoids rebuilding unchanged
   application artifacts.

### 2. Candidate Publication

1. Create signed annotated tag `v2.14.0-rc.1` at the reviewed `main` revision.
2. Require the complete tag workflow to build and publish both API and Web
   images from that exact revision.
3. Record and independently verify both immutable digests, OCI revision
   metadata, and published-image smoke jobs.
4. Do not create a mutable alias or stable GitHub Release at this stage.

The tag workflow is the one intentional full validation pass after repository
closure. It requires no interactive production authorization while it builds
and verifies the images.

### 3. Digest-Pinned Rollout

1. Retain the current v2.13 API and Web digests in owner-only rollback
   configuration.
2. Capture a sanitized continuity baseline for public health, application
   container identities and restart counts, A2S state, incidents, durable
   queues, incomplete commands, and scheduled-backup freshness.
3. Run production preflight against the verified candidate digests.
4. Recreate only API and Web. Do not recreate Caddy, PostgreSQL, telemetry, or
   the game service, and do not run a schema-changing operation.
5. Do not provision an identity, activate the game-event agent, install a
   plugin, start a producer or delivery path, or manufacture gameplay data.

### 4. Short Production Smoke

Collect at least three healthy read-only samples spanning at least three
minutes and stop on the first unexplained failure:

1. Require public API liveness, API readiness, Web health, and the Web root to
   return HTTP `200`, with candidate version, revision, and exact image
   digests.
2. Require no unexpected API or Web restart and preserve the baseline for all
   untouched containers and the game service.
3. Require the controlled server to remain recently polled and A2S-reachable
   with zero bots. Require no newly opened incident, pending-outbox growth,
   dead letter, or incomplete command. Do not drain reviewed deferred work to
   make the smoke pass.
4. Through an existing authenticated Reader-capable session, open Recent
   Activity, select the controlled server, switch among All, Incidents,
   Commands, and Rounds, verify Refresh preserves that scope, and verify
   `Clear server` removes it while retaining the selected type.
5. Verify the selected-server counts remain internally consistent and a type
   filter never displays a row from another source or server. A naturally
   empty type is acceptable; do not manufacture production data for this gate.
6. Confirm the page and browser storage contain no bearer token, credential,
   secret reference, raw database row, address, or unauthorized mutation
   control.
7. Recheck public health and scheduled-backup freshness in the final sample.

This smoke does not repeat a game-host continuity interval, soak, backup or
restore rehearsal, migration rehearsal, alert-outbox investigation, Auth0
reconfiguration, or gameplay-delivery exercise. Existing passive monitoring
continues independently.

## Failure And Rollback Rules

- Do not deploy if publication, digest verification, preflight, or the
  continuity baseline fails.
- On startup, health, authorization, filtering, count consistency, navigation,
  or rendering failure, recreate API and Web together from the retained v2.13
  digests.
- Do not mutate PostgreSQL, monitoring state, game-event state, RCON state,
  incident history, command history, or durable queues to make the smoke pass.
- Store sanitized production evidence outside Git as an owner-only regular
  file.

## Stable Publication Gate

Stable `v2.14.0` remains blocked until all four gates pass:

1. The candidate identity table is complete from verified publication output.
2. The digest-pinned rollout and short production smoke pass.
3. Production has either retained the candidate or cleanly rolled back with a
   documented reason.
4. A signed stable tag promotes the exact accepted API and Web digests without
   rebuilding them, and both stable references pass published-image smoke.

Candidate acceptance and stable-publication evidence belong in one short
follow-up pull request after both remote stages complete.

## References

- [v2.14 release notes](release-notes-v2.14.md)
- [Scoped Activity Investigation pull request](https://github.com/tov-vl/gold-src-ops/pull/166)
- [Product pull request workflow](https://github.com/tov-vl/gold-src-ops/actions/runs/35204950777)
- [Product post-merge workflow](https://github.com/tov-vl/gold-src-ops/actions/runs/35205688559)
- [Release process](release-process.md)
- [v2.13 release readiness](v2.13-readiness.md)
- [Project backlog](backlog.md)
- [Project brief](project-brief.md)
- [Deployment](deployment.md)
- [Production Compose contract](../ops/production/README.md)
- [Security](security.md)
