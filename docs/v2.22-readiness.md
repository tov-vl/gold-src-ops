# GoldSrcOps v2.22 Release Readiness

Review date: 2026-09-22. Status: released. The End-to-End Delivery Readiness
slice was published from exact revision
`7c4d6f63f756f702693281f1a34c3f0c8315adce`, accepted on the target, and
promoted without rebuilding any candidate image.

## Scope

The v2.22 release scope is End-to-End Delivery Readiness:

- the configured receiver `CatchUp` or `Live` mode in the existing sanitized
  private provider-delivery status;
- server-side composition of the existing source alert-delivery snapshot and
  receiver provider-delivery snapshot;
- one Operator-only delivery chain for the control-plane sender, receiver mode,
  and provider worker;
- explicit `Safely paused`, `Partially active`, `Action required`, and `Live`
  classifications;
- fail-closed rendering of an unavailable receiver mode as `Unknown` and
  `Partially active` during a mixed-version interval;
- responsive desktop and mobile presentation while preserving the existing
  provider queue and immutable review workflow.

The view is read-only. It cannot retry, replay, delete, reclassify, unblock,
review, enable, or reconfigure delivery work.

## Claim Boundary

Candidate acceptance can show that the exact Web and AlertReceiver artifacts
compose naturally existing source and provider delivery state for an
authenticated Operator, classify the three-stage chain according to the
reviewed rules, expose only sanitized aggregate fields, and leave the receiver
and both delivery workers in their pre-rollout modes. It can also show public
health, exact image identity, unchanged durable queue and review state, and
continuity of unaffected runtime.

Acceptance cannot prove notification delivery, escalation, provider
reachability, worker liveness, throughput, retry correctness under failure,
sustained operation, high availability, long-term reliability, or an achieved
SLO. It must not enable a worker, change receiver mode, create or mutate queue
rows, or alter reviews or provider state to manufacture a chain state.

## Frozen Release Inputs

| Property | Candidate value |
| --- | --- |
| Candidate version | `v2.22.0-rc.1` |
| Stable version | `v2.22.0` |
| Product merge | PR [#203](https://github.com/tov-vl/gold-src-ops/pull/203), merge `cda20473118c01d30e058a7ebd7ec89d8b258be2` |
| Candidate source revision | Readiness PR [#204](https://github.com/tov-vl/gold-src-ops/pull/204), merge `7c4d6f63f756f702693281f1a34c3f0c8315adce` |
| Risk class | R1: additive read-only Web and internal receiver behavior |
| Published artifacts | API `sha256:9e88fb28ebdf5f372a7cef556de443252a9a2bb6ca44e63269d354498406814c`; Web `sha256:5e573fd1ecb3e0a42a4743e289bca82067417a783340f293df77349778d62e3b`; AlertReceiver `sha256:5a40a4a97ea111e0aafee15ab174edf8a11271e60603b4320391d2e04c69b867` |
| Affected target runtime | Web and AlertReceiver on the existing control-plane deployment |
| Unaffected runtime | API process, both PostgreSQL schemas, Caddy routes, telemetry, identity configuration, game host, and external monitoring |
| Previous rollback boundary | Accepted v2.21 Web and AlertReceiver digests and deployment inputs retained owner-only |
| Receiver mode | `CatchUp` throughout rollout and acceptance |
| Source sender state | `AlertDelivery__Enabled=false` throughout rollout and acceptance |
| Provider worker state | `ProviderDelivery__Enabled=false` throughout rollout and acceptance |

Changing the version, frozen source revision, runtime scope, risk class,
authorization identity, receiver mode, or disabled-worker boundary creates a
new release plan.

## Candidate Diff Inventory

The final freeze must compare the exact readiness-merge revision with signed
stable `v2.21.0`. The current inventory is:

| Change | Current identity | Runtime significance |
| --- | --- | --- |
| v2.21 stable evidence closure | PR [#201](https://github.com/tov-vl/gold-src-ops/pull/201), merge `a3cd463` | Documentation only |
| Stable-promotion CI optimization | PR [#202](https://github.com/tov-vl/gold-src-ops/pull/202), merge `3bcd3af` | Release workflow and verification tooling only |
| End-to-End Delivery Readiness | PR [#203](https://github.com/tov-vl/gold-src-ops/pull/203), merge `cda2047` | Additive receiver mode projection and Operator-only Web composition |
| v2.22 readiness package | PR [#204](https://github.com/tov-vl/gold-src-ops/pull/204), merge `7c4d6f6` | Candidate identity, R1 rollout, rollback, acceptance, and claim limits |

No additional runtime commit entered the frozen revision after PR #203.

## Compatibility Review

- Neither the control-plane nor receiver database has a v2.22 migration,
  model change, backfill, or data correction.
- The receiver adds `ReceiverMode` to its existing internal status response.
  A v2.21 Web ignores the additive field.
- A v2.22 Web treats a missing or unknown receiver mode as `Unknown` and
  `Partially active`; it cannot falsely report a mixed-version chain as safely
  paused or live.
- Replace AlertReceiver before Web to avoid that temporary unknown state. No
  queue or provider mutation is possible in either order.
- The Web BFF requests both protected status snapshots server-side. Browser
  code receives neither receiver authorization nor a new public endpoint.
- The existing provider-operations authorization, public Caddy routes, roles,
  identity-provider configuration, and delivery settings are unchanged.
- Rollback restores the accepted v2.21 Web and AlertReceiver digests without a
  database, queue, authorization, API, Caddy, telemetry, identity, receiver
  mode, or game-host action.

## Repository Gates

| Gate | Current result |
| --- | --- |
| Receiver mode projection and sanitized private status | Passed in required product CI on a clean runner |
| Server-side composition, four chain classifications, and mixed-version fail-closed behavior | Passed locally and in required product CI |
| Desktop/mobile delivery-chain rendering and browser token boundary | Passed locally and in `Browser Smoke`; an initial misplaced assertion was corrected before merge |
| Complete solution Quality Gate, package audit, container smoke, and browser smoke | Passed in the final product PR workflow [#35637975342](https://github.com/tov-vl/gold-src-ops/actions/runs/35637975342) |
| Product post-merge `main` CI | Passed `Change Scope`, `Quality Gate`, `Container Smoke`, and `Browser Smoke` in workflow [#35639230653](https://github.com/tov-vl/gold-src-ops/actions/runs/35639230653) |
| Documentation validation | Passed locally for 92 Markdown files and 516 local links |
| Readiness pull request and documentation-only CI | Passed in PR [#204](https://github.com/tov-vl/gold-src-ops/pull/204), workflow [#35643396446](https://github.com/tov-vl/gold-src-ops/actions/runs/35643396446), and post-merge workflow [#35645157060](https://github.com/tov-vl/gold-src-ops/actions/runs/35645157060) |
| Signed candidate tag and independently verified API/Web/AlertReceiver digests | Passed for `v2.22.0-rc.1` in [candidate workflow #35651626318](https://github.com/tov-vl/gold-src-ops/actions/runs/35651626318) |
| Digest-pinned rollout and bounded production acceptance | Passed: AlertReceiver then Web only; exact digests, zero unexpected restarts, unchanged durable queue/review state, receiver `CatchUp`, both workers disabled, and three healthy samples spanning 181 seconds |
| Signed stable tag and digest-preserving promotion | Passed in [stable workflow #35659136810](https://github.com/tov-vl/gold-src-ops/actions/runs/35659136810); all three builds were skipped and all three stable digest smokes passed |
| Stable project release | Published as [GoldSrcOps v2.22.0](https://github.com/tov-vl/gold-src-ops/releases/tag/v2.22.0) |

Do not repeat the complete application suite on this unchanged runtime revision
merely to duplicate the green product and post-merge workflows. Investigate any
disagreement between repository, tag, artifact, and target evidence.

## Accepted Release Sequence

### 1. Merge Readiness And Freeze

1. Merge this documentation-only readiness record through protected `main`
   after its required checks pass.
2. Record the exact readiness merge SHA and retain the green product
   post-merge workflow `#35639230653` as the application gate.
3. Confirm `v2.22.0-rc.1` and `v2.22.0` remain unused and compare the final
   inventory with signed stable `v2.21.0`.
4. Retain the accepted v2.21 Web and AlertReceiver digests, owner-only
   environments, authorization file, and rollback commands outside Git.

### 2. Publish And Verify The Candidate

1. Create, locally verify, and push signed annotated tag `v2.22.0-rc.1` at the
   exact reviewed `main` revision.
2. Require the complete tag workflow followed by publication and independent
   digest verification for every workflow image.
3. Verify OCI source, revision, version, license, and immutable Web and
   AlertReceiver digests before preparing a target mutation.
4. Stop before rollout if artifact identity differs from the frozen revision.

### 3. Capture Baseline And Roll Out

1. Capture sanitized API, Web, receiver, source queue, provider queue, review,
   incident, backup, A2S, game-service, image, and restart baselines. Keep
   topology and raw rows owner-only.
2. Do not run a backup/restore rehearsal, migration action, game-host action,
   queue drain, receiver-mode change, or delivery operation for this R1 release.
3. Replace AlertReceiver and then Web with their independently verified
   candidate digests. Recreate no other component.
4. Keep the existing operations authorization unchanged, the receiver in
   `CatchUp`, and both delivery workers disabled.

### 4. Accept Or Roll Back

1. Collect at least three healthy read-only samples spanning at least three
   minutes for public API/Web health, receiver health, exact candidate identity,
   restart counts, and dependent runtime continuity.
2. Through an existing Operator session, verify the delivery chain matches
   bounded owner-only source and provider evidence and exposes no credential,
   payload, raw provider response, or mutation control.
3. Confirm Reader access remains forbidden and the provider-operations route
   remains absent from public Caddy.
4. Confirm source and provider pending, processing, dead-letter, and review
   state did not change as a side effect of observation; both workers remain
   disabled and the receiver remains in `CatchUp`.
5. On failure, restore only Web and AlertReceiver to the recorded v2.21
   digests. No database, authorization, queue, review, API, Caddy, telemetry,
   identity, receiver-mode, or game-host rollback is part of this release.

### 5. Promote And Close

Signed stable tag `v2.22.0` resolves to the exact accepted `rc.1` revision.
[Workflow #35659136810](https://github.com/tov-vl/gold-src-ops/actions/runs/35659136810)
used the stable-promotion fast path, skipped the API, Web, and AlertReceiver
build paths, promoted the accepted digests unchanged, and independently
smoke-tested all three stable references. The
[GitHub Release](https://github.com/tov-vl/gold-src-ops/releases/tag/v2.22.0)
is published. No production runtime was changed during stable promotion.

## Failure Rules

- Stop on the first unexplained CI, artifact, health, authorization, route,
  queue, review, identity, receiver-mode, or continuity failure.
- Never retry an uncertain mutation, replace a published tag, rebuild an
  accepted image, expose the operations route publicly, or enable a delivery
  worker to make acceptance pass.
- Do not create or alter durable data or configuration to manufacture a chain
  state.
- Stop for a new decision if the runtime scope, risk class, secret identity,
  receiver mode, worker state, or rollback boundary changes.

## References

- [v2.22 release notes](release-notes-v2.22.md)
- [End-to-End Delivery Readiness pull request](https://github.com/tov-vl/gold-src-ops/pull/203)
- [Product CI](https://github.com/tov-vl/gold-src-ops/actions/runs/35637975342)
- [Product post-merge CI](https://github.com/tov-vl/gold-src-ops/actions/runs/35639230653)
- [Release readiness pull request](https://github.com/tov-vl/gold-src-ops/pull/204)
- [Candidate workflow](https://github.com/tov-vl/gold-src-ops/actions/runs/35651626318)
- [Stable workflow](https://github.com/tov-vl/gold-src-ops/actions/runs/35659136810)
- [GoldSrcOps v2.22.0](https://github.com/tov-vl/gold-src-ops/releases/tag/v2.22.0)
- [Release process](release-process.md)
- [Deployment](deployment.md)
- [AlertReceiver deployment contract](../ops/alert-receiver/README.md)
- [Security](security.md)
- [Project backlog](backlog.md)
- [v2.21 readiness](v2.21-readiness.md)
