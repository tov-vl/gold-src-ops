<nav class="section-nav" aria-label="Server views">
    <NavLink href="@($"/operator/servers/{ServerId:D}")" Match="NavLinkMatch.All">Overview</NavLink>
    <NavLink href="@($"/operator/servers/{ServerId:D}/history")">History</NavLink>
    <NavLink href="@($"/operator/servers/{ServerId:D}/commands")">Commands</NavLink>
    <NavLink href="@($"/operator/servers/{ServerId:D}/settings")">Configuration</NavLink>
    <NavLink href="@($"/operator/servers/{ServerId:D}/credentials")">Credentials</NavLink>
</nav>

@code {
    [Parameter]
    public Guid ServerId { get; set; }
}
